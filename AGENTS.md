# AGENTS.md

## 目的

このリポジトリでは、プルリクエスト（PR）に対して Codex による自動レビューを行います。  
レビューは **日本語** で実施し、再現可能な改善提案と具体的なコード例を提示してください。

## 言語ポリシー（必須）

- すべてのレビューコメントは **日本語** で記述してください。
- 専門用語は原語（英語）併記可（例: 「メモリリーク（memory leak）」）。
- 断定ではなく、根拠と代替案を伴う提案の形で記述してください。

## レビュー方針（優先度順）

1. **正確性・安全性**：バグ、例外処理欠落、セキュリティ脆弱性（入力検証・認可・秘密情報漏えい・依存ライブラリの既知脆弱性）。
2. **パフォーマンス**：計算量、不要な I/O、N+1、非効率なループ・データ構造。
3. **アーキテクチャ整合**：層境界、責務分離、循環依存の回避。
4. **テスト容易性**：テストカバレッジ、モック可能性、境界値。
5. **可読性・メンテナンス性**：命名、関数長、重複、コメントの妥当性。

## 出力フォーマット

- PR ごとに **要約** → **主要指摘（箇条書き）** → **詳細** → **修正例（差分またはコードブロック）** の順でコメントしてください。
- 重大度ラベルを付与：`[Critical] / [High] / [Medium] / [Low]`
- 各指摘には根拠（規約・原則・計測）を簡潔に添付してください。

